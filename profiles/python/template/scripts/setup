#!/usr/bin/env bash
set -euo pipefail

# scripts/setup (python)
# 说明：创建 .venv 并安装依赖（requirements + dev requirements）

python3 -m venv .venv
source .venv/bin/activate

python -m pip install -U pip
python -m pip install -r requirements.txt -r requirements-dev.txt

echo "[setup] OK"
