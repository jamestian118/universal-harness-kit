name: gc
on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 1"

jobs:
  gc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: chmod scripts
        run: chmod +x ./scripts/* || true
      - name: gc
        run: ./scripts/gc --strict
